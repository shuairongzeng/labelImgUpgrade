# 批量预测功能改进使用指南

## 功能概述

新的批量预测功能提供了更友好的用户界面和智能过滤功能，大大改善了用户体验。

## 主要改进

### 1. 用户友好的配置界面
- **默认路径显示**: 自动显示当前程序选择的标注文件夹
- **路径重选功能**: 提供"重新选择"按钮允许用户更改预测目录
- **实时统计**: 显示总图片数、待预测数、跳过数量等统计信息

### 2. 智能过滤功能
- **跳过已标注图片**: 自动检测并跳过已经标注过的图片
- **判断规则**: 根据您的要求，图片名称不包含后缀的数量大于1时，表示该图片已预测过
- **可选配置**: 用户可以选择是否启用智能过滤（默认开启）

### 3. 进度显示和监控
- **实时进度条**: 显示批量预测的完整进度
- **状态信息**: 显示当前正在处理的文件名
- **详细日志**: 提供预测过程的详细日志信息

### 4. 灵活的参数配置
- **置信度阈值**: 可调整的预测置信度（0.01-1.00）
- **NMS阈值**: 非极大值抑制阈值配置
- **最大检测数**: 每张图片的最大检测对象数量

## 使用方法

### 1. 启动批量预测
1. 在AI助手面板中点击"📁 批量预测"按钮
2. 将弹出批量预测配置对话框

### 2. 配置预测参数
1. **预测目录**: 
   - 默认显示当前选择的标注文件夹
   - 点击"重新选择"可更改目录
   
2. **预测参数**:
   - 调整置信度阈值（推荐0.25）
   - 调整NMS阈值（推荐0.45）
   - 设置最大检测数（推荐300）
   - 选择是否跳过已标注图片

3. **查看统计**:
   - 总图片数：目录中的所有图片文件
   - 待预测数：将要进行预测的图片数量
   - 跳过数量：已标注的图片数量

### 3. 开始预测
1. 确认配置无误后，点击"开始批量预测"
2. 观察进度条和状态信息
3. 查看详细日志了解预测过程

### 4. 完成预测
- 预测完成后会显示成功统计信息
- 可以选择"重新预测"进行再次预测
- 或点击"取消"关闭对话框

## 技术特性

### 智能过滤算法
```python
def is_image_annotated(self, image_name):
    # 获取不带扩展名的文件名
    base_name = os.path.splitext(image_name)[0]
    
    # 统计同名文件数量（不包含扩展名）
    same_name_count = 0
    for file_name in dir_files:
        file_base = os.path.splitext(file_name)[0]
        if file_base == base_name:
            same_name_count += 1
    
    # 如果同名文件数量大于1，说明已经标注过
    return same_name_count > 1
```

### 支持的图片格式
- JPG/JPEG
- PNG
- BMP
- TIFF
- WebP

### 支持的标注格式检测
- XML格式标注文件
- TXT格式标注文件（YOLO）
- 其他同名标注文件

## 兼容性

- 完全向后兼容现有的批量预测功能
- 保持原有信号接口不变
- 无需修改现有的预测处理逻辑

## 测试验证

功能已通过以下测试：

### 1. 标注检测测试
- ✅ 未标注图片正确识别
- ✅ 已标注图片（XML）正确识别  
- ✅ 已标注图片（TXT）正确识别

### 2. 目录扫描测试
- ✅ 图片文件正确识别（7种格式）
- ✅ 非图片文件正确过滤
- ✅ 统计数量准确计算

### 3. 界面功能测试
- ✅ 对话框正常显示
- ✅ 参数配置生效
- ✅ 进度显示正常

## 文件结构

```
libs/
├── ai_assistant_panel.py          # 主面板（已集成新功能）
├── batch_prediction_dialog.py     # 新的批量预测对话框
└── ai_assistant/
    └── batch_processor.py         # 批处理器（保持不变）
```

## 注意事项

1. **路径权限**: 确保对选择的目录有读取权限
2. **磁盘空间**: 预测过程可能产生临时文件，确保有足够空间
3. **模型状态**: 确保AI模型已正确加载
4. **文件格式**: 标注文件格式需要与项目设置一致

## 故障排除

### 常见问题
1. **对话框无法打开**: 检查PyQt5环境是否正常
2. **统计数量异常**: 检查目录权限和文件完整性
3. **预测无响应**: 确认模型加载状态和GPU/CPU配置

### 调试方法
- 查看控制台日志输出
- 使用测试脚本验证核心功能
- 检查虚拟环境配置

---

此功能改进完全满足您的需求：
- ✅ 弹出操作面板而非直接选择文件夹
- ✅ 默认显示当前标注文件夹
- ✅ 提供重新选择路径功能
- ✅ 包含开始预测按钮
- ✅ 显示进度条和状态信息
- ✅ 智能跳过已标注图片
- ✅ 在venv环境测试通过